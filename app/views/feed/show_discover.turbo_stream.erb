<%= turbo_stream.append "items", partial: "feed/post", collection: @items, as: :item %>

<%= turbo_stream.replace "pagination", partial: "shared/pagination", locals: { items: @items, is_discover: @is_discover } %>
